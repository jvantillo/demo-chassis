apiVersion: 1

providers:
- name: 'Provisioned'
  folder: 'Provisioned'
  type: file
  disableDeletion: false
  editable: true
  updateIntervalSeconds: 10
  options:
    path: /etc/grafana/provisioning/dashboards
